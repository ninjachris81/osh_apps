<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.MainActivity">

    <item
        android:id="@+id/menu_settings"
        app:materialIcon="settings"
        app:showAsAction="never"
        android:title="@string/settings" />

    <item
        android:id="@+id/exitapp"
        android:title="@string/exitapp"
        app:materialIcon="exit_to_app"
        app:showAsAction="never" />

    <item android:id="@+id/sip_connected_state_icon"
        android:title=""
        app:materialIcon="phone"
        app:showAsAction="always"/>

    <item android:id="@+id/db_connected_state_icon"
        android:title=""
        app:materialIcon="database"
        app:showAsAction="always"/>

    <item android:id="@+id/mqtt_connected_state_icon"
        android:title=""
        app:materialIcon="wifi_off"
        app:showAsAction="always"/>



</menu>